You need the Gnome-Color-Configurator to change the theme appearance.
Then you are able to import this configuration file...

Enter in a shell:

> sudo apt-get update && sudo apt-get install gnome-color-chooser -f -y
